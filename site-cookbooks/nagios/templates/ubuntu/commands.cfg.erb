
define command{
	command_name	check_x224
	command_line	/usr/lib/nagios/plugins/check_x224 -H $HOSTNAME$
}

define command{
	command_name	check_x224_port
	command_line	/usr/lib/nagios/plugins/check_x224 -H $HOSTNAME$ -p $ARG1$
}

define command{
	command_name	check_nfs
	command_line	/usr/lib/nagios/plugins/check_nfs $HOSTNAME$ $ARG1$ $ARG2$ $ARG3$
}

define command {
	command_name	check_git_freshness
	command_line	/usr/lib/nagios/plugins/check_git_freshness --git-dir $ARG1$ --freshness $ARG2$
}

define command{
	command_name    check_borubot_sensor_fetcher
	command_line    /usr/lib/nagios/plugins/check_by_ssh -l pi -H '$HOSTADDRESS$' -C "ps aux|grep -v grep|grep 'borubot sensor-fetcher' || exit 2"
}

define command {
	command_name    check_borubot_worker
	command_line    sh -c 'ps aux|grep -v grep|grep resque|grep "Waiting for borubot" || exit 2'
}